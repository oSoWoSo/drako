# dR4ko - Core Profile
# This file contains the default commands for the Core profile.

# ┌─ Profile Settings ───────────────────────────────────────┐
# │ Customize the look and feel of this profile.
# └──────────────────────────────────────────────────────────┘
x = 3
y = 3

#header_art = '''
#╔══════════════════════════════════════╗
#║  Core Profile                        ║
#╚══════════════════════════════════════╝
#'''

# ┌─ Dynamic Header Command ──────────────────────────────────────────────────┐
# │ Per-profile header command. Overrides global setting.                     │
# │ WARNING: Command runs on every render - keep it fast!                     │
# └────────────────────────────────────────────────────────────────────────────
#header_command_enabled = false
#header_command = "fortune"

#==========================================
# ┌─ Column 0: System & Security ──────────┐
# └────────────────────────────────────────┘
[[commands]]
name = "⬆️ System Update"
description = "Queries the repositories for fresh versions and pulls them into your system. Your digital infrastructure sheds old scales for new ones—staying sharp, staying current. The machinery needs to evolve or rust settles in."
command = "{{⬆️ System Update}}"
col = "A"
row = 1
auto_close_execution = false

[[commands]]
name = "🧹 Maintenance ⋮ "
description = "Maintenance and threat scanning. Clear out the debris, hunt for what shouldn't be there. A lean system moves faster."
col = "A"
row = 2
items = [
    { name = "Clean Packages", description = "Clears out the old scales your system sheds during updates. Removes what's no longer needed so nothing rots in the corners.", command = "{{Clean Packages}}", auto_close_execution = false },
    { name = "Virus Scan (Home)", description = "Updates threat signatures and prowls through your home directory. Hunting for what doesn't belong.", command = "{{Virus Scan (Home)}}", auto_close_execution = false }
]

[[commands]]
name = "👀 Process Monitor"
description = "Everything running right now, laid bare. Watch processes pulse, memory flow, CPU cycles burn. The system breathing beneath the surface. See what's moving in real-time."
command = "{{👀 Process Monitor}}"
col = "A"
row = 3

#==========================================
# ┌─ Column 1: Files & Info ───────────────┐
# └────────────────────────────────────────┘
[[commands]]
name = " 🔎 File Finder"
description = "Type and watch it hunt through thousands of files instantly. When your filesystem sprawls like endlessly, this cuts straight to what you're tracking. Fast, precise, relentless."
command = "{{ 🔎 File Finder}}"
col = "B"
row = 1
auto_close_execution = false

[[commands]]
name = "🌐 Bandwidth"
description = "Curious how much internet you're using right now? This opens up a little dashboard showing data going in and out. I find it mesmerizing to watch the numbers jump when I'm downloading a big file."
command = "{{🌐 Bandwidth}}"
col = "B"
row = 2


[[commands]]
name = "📊 System Status ⋮ "
description = "Vital signs and stats. Hardware details, disk territories mapped, or the weather beyond your screen. Information at different scales."
col = "B"
row = 3
items = [
    { name = "System Info", description = "The bones of your machine. Distro, kernel version, uptime, hardware beneath the surface. See what you're running on.", command = "{{System Info}}", auto_close_execution = false },
    { name = "Disk Usage", description = "Map of your storage. See where space went, find what's been forgotten in dusty corners.", command = "{{Disk Usage}}", auto_close_execution = false },
    { name = "Weather", description = "Quick weather from wttr.in. The world beyond the screen, right here in the terminal.", command = "{{Weather}}", auto_close_execution = false }
]

#debug_output = true TODO
#==========================================
# ┌─ Column 2: Network & Web ──────────────┐
# └────────────────────────────────────────┘
[[commands]]
name = " 📦 Install Tools ⋮ "
col = "C"
row = 1
description = "Bundled up all the little tools that this menu uses, just in case you don't have them. It's like getting all the batteries and accessories included for once. This makes sure all the other buttons here will work as expected. I like to be prepared, you know? "
items = [
    { name = "Install common tools", description = "The core utilities—btop, neofetch, clamav, fzf, bmon. Tools that keep the machinery visible and responsive.", command = "{{Install common tools}}", auto_close_execution = false},
    { name = "Install micro", description = "Installs micro, a modern terminal text editor. Clean interface, easy to use. Set it as your default EDITOR via Settings if you want.", command = "{{Install micro}}", auto_close_execution = false},

    { name = "🃏 Summon '101 Series' Command Deck", description = "Pulls the 101 deck from the remote archives into your inventory. New space to explore.", command = "{{🃏 Summon '101 Series' Command Deck}}", auto_close_execution = false},
    
    { name = "🃏 Summon 'ggml' Command Deck", description = "Pulls the ggml deck from the remote archives into your inventory. Start learning about llama.cpp.", command = "{{🃏 Summon 'ggml' Command Deck}}", auto_close_execution = false},
    
    { name = "🃏 Summon Private Deck", description = "Pulls a private deck from locked archives. Requires SSH keys to access the hidden chambers.", command = "{{🃏 Summon Private Deck}}", auto_close_execution = false},
]

[[commands]]
name = "📜 Open Shell Config"
description = "This is where all your terminal customizations live. The shell config file loads every time you open a terminal and controls your aliases, environment variables, and prompt appearance. It's like the settings panel for your command line experience. I open this file whenever I want to add a new shortcut or tweak how my terminal behaves."
command = "{{📜 Open Shell Config}}"
col = "C"
row = 2


[[commands]]
name = " 🐉 Settings ⋮ "
col = "C"
row = 3
description = "The dragon's lair. Tweak the internal mechanics, adjust your environment, and refine how you interact with the system. This is where you make the machine yours."
items = [
    { name = "⚙️  Edit config.toml ", description = "The file you're reading right now. Key bindings, themes, etc.—all controlled here. Change anything. Break things. That's the point. Power lives in plain text files. Rescue mode can help you restore a broken config.", command = "{{⚙️  Edit config.toml}}"},
    { name = "📁 Browse config dir", description = "Hidden chambers where configurations live. Most users never look here, but this is where real control exists. Explore it. Break things. Make it yours. The vastness of cyber starts in folders like these.", command = "{{📁 Browse config dir}}"},
    { name = "ℹ️  Open Pivot  ", description = "Small scale state tracking, profile switches, paths. Useful for debugging or just curiosity.", command = "{{ℹ️  Open Pivots}}"},
    { name = "📜 Open History Logs", description = "A record of past movements. Every command executed, every choice made, preserved in the archives. Peer into the past to trace your steps.", command = "{{📜 Open History Logs}}" },
    { name = "📜 Open Logs (Debug)", description = "The trail of what happened. Events, errors, state changes recorded. Read the logs when things break or when you want to see the machinery.", command = "{{📜 Open Logs}}"},

    #===========================================================================================================
    { name = "Reload Shell", description = "NOTE: Won't affect your current shell (child process limitation). Use this to copy the command: press 'y', then paste it into your terminal to actually reload.", command = "{{Reload Shell}}", auto_close_execution = false},
    { name = "Add go/bin to PATH (<-- Very Recommended)", description = "Adds ~/go/bin to your PATH so Go binaries run from anywhere. Makes the tools globally accessible.", command = "{{Add go/bin to PATH}}", auto_close_execution = false},
    { name = "Alias x=drako (<-- Recommended)", description = "Adds 'alias x=drako' to ~/.bashrc. Launch with just 'x'. Quick access to the den.", command = "{{Alias x=drako}}" , auto_close_execution = false},
    { name = "Set micro as EDITOR (<-- Recommended)", description = "Sets EDITOR environment variable to micro in ~/.bashrc. Your default editor for system commands. Restart shell after.", command = "{{Set micro as EDITOR}}", auto_close_execution = false},
]
