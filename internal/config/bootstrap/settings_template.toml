# dR4ko - Global Settings

# ┌─ Theme ──────────────────────────────────────────────────────────┐
# │ Select the color palette for the UI.
# │ Available themes:
# └──────────────────────────────────────────────────────────────────┘

theme = "dracula"
#theme = "jade"
#theme = "nord"
#theme = "dracula2"
#theme = "everforest"
#theme = "orasaka"

# ┌─ Auto Lock Session ──────────────────────────────────────────────────┐
# │ When enabled, drako will automatically lock after a period of inactivity.
# │ This prevents accidental key presses after a period of idle periods.
# │
# │ Recommended: Keep it enabled (default)
# └────────────────────────────────────────────────────────────┘
#auto_lock_enabled = false
#lock_timeout_minutes = 1 #

# ┌─ Key Bindings ─────────────────────────────────────────────┐
# │ Customize your control scheme.
# │ Arrows are always enabled.
# │ Defaults are shown commented out.
# └────────────────────────────────────────────────────────────┘

# ┌─ Keybinding Modifier ────────────────────────────────────┐
# │ Set the modifier for number-based shortcuts.
# │ "alt" is the default and generally works well.
# │ "ctrl" is another option, but may be intercepted by your
# │ terminal or window manager.
# │ Function keys (F1-F9) are not configured via this string.
# └───────────────────────────────────────────────────────────┘
numb_modifier = "alt"

[keys]
#disable_wasd_bindings = true
#disable_vim_bindings = true

#explain = "e"
#inventory = "i"
#path_grid_mode = "tab"
#lock = "r"
profile_prev = "o"
profile_next = "p"

# ┌─ Environment Variables ──────────────────────────────┐
# | By default, drako inherits your full shell environment
# | to ensure maximum compatibility with your tools.
# |
# | Uncomment 'env_whitelist' below to enable strict mode.
# | Only the listed variables will be passed to commands.
# | This is recommended for high-security setups.
# └──────────────────────────────────────────────────────┘
# env_whitelist = [
#     "PATH",
#     "HOME",
#     "USER",
#     "SHELL",
#     "TERM",
#     "LANG",
#     "LC_ALL",
#     "EDITOR",
#     "VISUAL",
#     "GOPATH",
#     "SSH_AUTH_SOCK"
# ]

# ┌─ Shell ────────────────────────────────────────┐
# | For each Profile (including the default), you
# | can set a custom shell to run the commands.
# | The default is bash, though some profiles are 
# | POSIX compatible by design, eg. for minimal
# | server distros.
# |
# | TODO: list them
# └────────────────────────────────────────────────

#default_shell = "bash"   # or "zsh", "fish", "sh", etc.

# ┌─ Dynamic Header Command ──────────────────────────────────────────────────┐
# │ Execute a command and display its output as the header.                    │
# │ Useful for showing system info, date, quotes, fortune, etc.               │
# │ WARNING: Command runs on every render - keep it fast!                     │
# └────────────────────────────────────────────────────────────────────────────
#header_command_enabled = false
#header_command = "date"
#header_command_args = ["+%H:%M:%S"]
#header_command_timeout = 2
#header_fallback = "dR4ko"
